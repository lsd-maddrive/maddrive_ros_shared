<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">

	<xacro:include filename="$(find maddrive_urdf_tools)/urdf/macros/inertia.urdf.xacro" />
	<xacro:include filename="$(find maddrive_urdf_tools)/urdf/macros/common.urdf.xacro" />

	<!-- BASE WHEEL -->
	<xacro:macro name="base_wheel" params="name">
		<link name="${name}">
			<visual>
				<origin xyz="0 0 0" rpy="${pi/2} 0 0" />
				<geometry>
					<cylinder length="${wheel_width}" radius="${wheel_radius}" />
				</geometry>
			</visual>
			<collision>
				<origin xyz="0 0 0" rpy="${pi/2} 0 0" />
				<geometry>
					<sphere radius="${wheel_radius}" />
					<!-- <cylinder length="${wheel_width}" radius="${wheel_radius}" /> -->
				</geometry>
			</collision>
			<xacro:cylinder_inertia r="${wheel_radius}" h="${wheel_width}" mass="${wheel_mass}">
				<origin xyz="0 0 0" rpy="${pi/2} 0 0" />
			</xacro:cylinder_inertia>
		</link>
		<gazebo reference="${name}">
			<material>Gazebo/Black</material>
			<selfCollide>false</selfCollide>
			<mu1 value="1.0" />
			<mu2 value="1.0" />
		</gazebo>
	</xacro:macro>

	<!-- SPEED WHEEL -->
	<xacro:macro name="speed_wheel" params="prefix parent origin_xyz ">

		<joint name="${prefix}_wheel_joint" type="continuous">
			<parent link="${parent}" />
			<child link="${prefix}_wheel_link" />
			<origin xyz="${origin_xyz}" rpy="0 0 0" />
			<axis xyz="0 1 0" />
			<limit effort="0" velocity="0" />
		</joint>

		<xacro:base_wheel name="${prefix}_wheel_link" />

	</xacro:macro>

	<!-- STEER WHEEL -->
	<xacro:macro name="steer_wheel" params="prefix parent steer_origin_xyz steer_limit">

		<joint name="${prefix}_steer_joint" type="revolute">
			<parent link="${parent}" />
			<child link="${prefix}_steer_link" />
			<origin xyz="${steer_origin_xyz}" rpy="0 0 0" />
			<axis xyz="0 0 1" />
			<limit lower="${-steer_limit}" upper="${steer_limit}" effort="0" velocity="0" />
		</joint>

		<link name="${prefix}_steer_link">
			<xacro:null_inertial />
		</link>

		<joint name="${prefix}_wheel_joint" type="continuous">
			<parent link="${prefix}_steer_link" />
			<child link="${prefix}_wheel_link" />
			<origin xyz="0 0 0" rpy="0 0 0" />
			<axis xyz="0 1 0" />
			<limit effort="0" velocity="0" />
		</joint>

		<xacro:base_wheel name="${prefix}_wheel_link" />

	</xacro:macro>
</robot>